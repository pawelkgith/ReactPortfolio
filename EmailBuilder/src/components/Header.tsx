import styles from '../scss/Header.module.scss';

function Header() {
    return (
        <div className={styles.header}>
            <h1>Email Builder</h1>
        </div>
    )
}

export default Header;